# Copy this file to .devcontainer/.env and set your Windows username.
# This is used to bind-mount Windows host folders into the devcontainer.
# The .env file is gitignored — each developer maintains their own copy.

WINDOWS_USER=
